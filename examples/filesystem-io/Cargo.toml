[package]
name = "filesystem-io"
version = "0.1.0"
edition = "2021"

[dependencies]
kos = { package = "kos-rs", git = "https://github.com/dreamcast-rs/kos-rs" }

[patch.crates-io]
libc = { path = "/opt/toolchains/dc/rust/libc" }
